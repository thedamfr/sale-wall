FROM castopod/castopod:latest

# Configuration pour CleverCloud
# CleverCloud attend que l'application écoute sur le port 8080
ENV PORT=8080

# Exposer le port 8080
EXPOSE 8080

# Castopod démarre son propre serveur web
# Pas besoin de modifier la configuration, on utilise le CMD par défaut de l'image
